<Window x:Name="Form" x:Class="SanityArchiver.DesktopUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SanityArchiver.DesktopUI.Views"
        xmlns:forms="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        mc:Ignorable="d"
        Title="Sanity Archiver" Height="450" Width="800">
    <Grid x:Name="MainWindow1" Loaded="Window_Loaded">


        <Grid.Resources>

            <HierarchicalDataTemplate DataType="{x:Type local:Directory}" ItemsSource ="{Binding Directories}">

                <TextBlock Text="{Binding Path=Name}" MouseDown="NameCol_mousedown"/>

            </HierarchicalDataTemplate>

        </Grid.Resources>

        <TreeView Margin="10,14,0,9.714" Name="treeView1" HorizontalAlignment="Left" Width="204" RenderTransformOrigin="0.5,0.5" >
            <TreeView.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform AngleY="-0.319"/>
                    <RotateTransform/>
                    <TranslateTransform Y="-0.569"/>
                </TransformGroup>
            </TreeView.RenderTransform>
            <TreeViewItem ItemsSource="{Binding Directories}" Header="Choose directory"/>
        </TreeView>
        <DataGrid x:Name="FilesDataGrid" AutoGenerateColumns="False" Margin="234,12,10.286,62.714">

            <DataGrid.Columns>
                <DataGridCheckBoxColumn x:Name="CheckBox" Header="" Binding="{Binding IsChecked}"/>
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding FileName}" Header="Name" Width="200"/>
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding Created}" Header="Created" Width="200"/>
                <DataGridTextColumn IsReadOnly="True" Binding="{Binding Size}" Header="Size(kb)" Width="200"/>
            </DataGrid.Columns>

        </DataGrid>

        <Button x:Name="CompressButton" Content="Compress" HorizontalAlignment="Left" Margin="234,364,0,30.714" Width="75" Click="CompressButton_Click"/>

        <Grid x:Name="CompressPopUp" Visibility="Hidden" Width="270" Height="150" HorizontalAlignment="Center" VerticalAlignment="Center" Background="LightCoral">
            <TextBox x:Name="CompressName" HorizontalAlignment="Center" VerticalAlignment="Top" >NameOfTheZip</TextBox>
            <Button x:Name="ZipButton" Click="ZipButton_Click" Width="50" Height="50" HorizontalAlignment="Left" Margin="50,0,0,0">Zip</Button>
            <Button x:Name="CompressCloseButton" Click="CompressCloseButton_Click" Width="50" Height="50" HorizontalAlignment="Right" Margin="0,0,50,0">Cancel</Button>
        </Grid>
        <Button Content="Encrypt" HorizontalAlignment="Left" Margin="314,362,0,0" VerticalAlignment="Top" Width="75" Height="27" Click="Encrypt"/>
        <Button Content="Decrypt" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Margin="709,362,0,0" Click="DecryptButton_Click"/>
    </Grid>

</Window>
